import Receipt from "@/components/screens/transfer/Receipt";
import { StyleSheet } from "react-native";

const Screen = () => {
  return <Receipt type="buy_fiat" />;
};

export default Screen;

const styles = StyleSheet.create({});
