import VerificationScreen from "@/components/screens/verifications";
import { StyleSheet } from "react-native";

const Scren = () => {
  return <VerificationScreen />;
};

export default Scren;

const styles = StyleSheet.create({});
