import NewPasswordScreen from "@/components/screens/settings/NewPasswordScreen";
import { StyleSheet } from "react-native";

const Screen = () => {
  return <NewPasswordScreen />;
};

export default Screen;

const styles = StyleSheet.create({});
