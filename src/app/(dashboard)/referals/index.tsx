import ReferralScreen from "@/components/screens/referrals";
import { StyleSheet } from "react-native";

const Screen = () => {
  return <ReferralScreen />;
};

export default Screen;

const styles = StyleSheet.create({});
